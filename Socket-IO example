


 { ServerSocket serverSocket = new ServerSocket("10.0.0.1", 3000);
	 Socket clientSocket = serverSocket.accept();
	 PrintWriter out = new PrintWroter(clentSocket.getOutputStream(), true);
	 BufferedReader in = new BufferedReader(new InputStreamReader(clientSocket.getInputStream())));
	 
     }
     
  { Socket wSocket = new Socket("10.0.0.1", 3000);
 	  PrintWriter out = new Printer Writer(wSocket.getOutputStream(), true);
 	  BufferedReader in = new BufferedReader(new InputStreamReader(wSocket.getInputStream()));
 	 
 	 }     



